<?xml version="1.0"?>

<!-- ======================================================== -->
<!--                 WOLips Ant file.                         -->
<!-- ======================================================== -->
<project name="woproject-wolips-devstudio" default="java" basedir=".">

    <!-- ============================================= -->
    <!-- Define CLASSPATH settings.                    -->
    <!-- ============================================= -->
    <target name="defineClasspath" if="wolips.build.devstudio">
       <path id="classpath">
            <fileset dir="${ant.home}/lib">
                <include name="ant.jar"/>
            </fileset>
            <fileset dir="lib">
                <include name="*.jar"/>
	            <include name="eclipse/**/*.jar"/>
            </fileset>
	    <fileset dir="${wolips.dist}">
			<include name="**/wolips.jar"/>
	    </fileset>
            <fileset dir="${wo.dir.library.frameworks}">
                <include name="JavaFoundation.framework/Resources/Java/*.jar"/>
            </fileset>
             <!-- Depends on WOProject core. -->
            <pathelement path="${build}/main"/>
        </path>
    </target>

    <target name="java" depends="prepare" if="wolips.build.support">
        <javac srcdir="src/wolips-devstudio/java"
                destdir="${build}/wolips-devstudio"
                deprecation="${compile.deprecation}"
                debug="${compile.debug}"
                optimize="${compile.optimize}">
            <classpath refid="classpath"/>
        </javac>
         
        <jar jarfile="${wolips.devstudio.dist}/lib/wolips-devstudio.jar">
            <fileset dir="${build}/wolips-devstudio"/>
            <fileset dir="src/wolips-devstudio/java">
          	    <include name="**/*.properties"/>
             </fileset>         
            <metainf dir="doc">
                <include name="LICENSE*"/>
            </metainf>
        </jar>
        
    </target>

    <target name="dist" depends="prepare,java,doc,api" if="wolips.build.devstudio">
        <copy todir="${wolips.devstudio.dist}">
             <fileset dir="src/wolips-devstudio">
             	<exclude name="**/java/**"/>
             	<exclude name="**/lib/**"/>
             	<exclude name="**/xdocs/**"/>
             	<exclude name="**/docs/**"/>
             	<exclude name=".project"/>
             	<exclude name=".classpath"/>
             	<exclude name="build.properties"/>
             </fileset>
         </copy>
      <jar jarfile="${wolips.devstudio.dist}.jar">
     	<fileset dir="${wolips.devstudio.dist}">
     	</fileset>
     </jar>
         
    </target>
         
         
         
    <!-- ============================================= -->
    <!-- Sets up build and distribution directories.   -->
    <!-- ============================================= -->
    <target name="prepare" depends="defineClasspath" if="wolips.build.devstudio">
        <mkdir dir="${build}/wolips-devstudio"/>
        <mkdir dir="${wolips.devstudio.dist}/lib"/>
    </target>


    <!-- ============================================= -->
    <!-- Deletes temporary files.                      -->
    <!-- ============================================= -->
    <target name="clean">
        <delete dir="${build}/wolips-devstudio"/>
        <delete dir="${wolips.devstudio.dist}"/>
    </target>
    
    
    <!-- ============================================= -->
    <!-- Deploys WOLips web site files in a local      -->
    <!-- directory.                                    -->
    <!-- ============================================= -->
    <target name="deploy-web" depends="prepare,site-doc" if="deploy.web"/>
    
    
    <!-- ========================================== -->
    <!-- Builds API documenatation.                 -->
    <!-- ========================================== -->
    <target name="api" depends="prepare" if="wolips.build.devstudio">
    </target>
    
    
    <target name="doc" depends="prepare" if="wolips.build.devstudio">
    </target>
    
    <target name="site-doc" depends="prepare" if="wolips.build.devstudio">
    </target>
</project>
