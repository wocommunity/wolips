<?xml version="1.0"?>

<!-- ======================================================== -->
<!--                 WOLips Ant file.                         -->
<!-- ======================================================== -->
<project name="woproject-wolips-targetbuilder" default="java" basedir=".">

    <!-- ============================================= -->
    <!-- Define CLASSPATH settings.                    -->
    <!-- ============================================= -->
    <target name="defineClasspath" if="wolips.build.targetbuilder">
        <path id="classpath">
            <fileset dir="${ant.home}/lib">
                <include name="ant.jar"/>
            </fileset>
            <fileset dir="lib">
                <include name="*.jar"/>
	            <include name="eclipse/**/*.jar"/>
            </fileset>
	    <fileset dir="${wolips.dist}">
			<include name="**/wolips.jar"/>
	    </fileset>
            <fileset dir="${wo.dir.library.frameworks}">
                <include name="JavaFoundation.framework/Resources/Java/*.jar"/>
            </fileset>
             <!-- Depends on WOProject core. -->
            <pathelement path="${build}/main"/>
        </path>
    </target>

    <target name="java" depends="prepare" if="wolips.build.targetbuilder">
        <javac srcdir="src/wolips-targetbuilder/java"
                destdir="${build}/wolips-targetbuilder"
                deprecation="${compile.deprecation}"
                debug="${compile.debug}"
                optimize="${compile.optimize}">
            <classpath refid="classpath"/>
        </javac>
         
        <jar jarfile="${wolips.targetbuilder.dist}/lib/wolips-targetbuilder.jar">
            <fileset dir="${build}/wolips-targetbuilder"/>
            <fileset dir="src/wolips-targetbuilder/java">
          	    <include name="**/*.properties"/>
             </fileset>         
            <metainf dir="doc">
                <include name="LICENSE*"/>
            </metainf>
        </jar>
        
    </target>

    <target name="dist" depends="prepare,java,doc,api" if="wolips.build.targetbuilder">
         <copy todir="${wolips.targetbuilder.dist}">
             <fileset dir="src/wolips-targetbuilder">
             	<exclude name="**/java/**"/>
             	<exclude name="**/lib/**"/>
             	<exclude name="**/xdocs/**"/>
             	<exclude name="**/docs/**"/>
             	<exclude name=".project"/>
             	<exclude name=".classpath"/>
             </fileset>
         </copy>
     <jar jarfile="${wolips.targetbuilder.dist}.jar">
     	<fileset dir="${wolips.targetbuilder.dist}">
     	</fileset>
     </jar>
          
    </target>
         
         
         
    <!-- ============================================= -->
    <!-- Sets up build and distribution directories.   -->
    <!-- ============================================= -->
    <target name="prepare" depends="defineClasspath" if="wolips.build.targetbuilder">
        <mkdir dir="${build}/wolips-targetbuilder"/>
        <mkdir dir="${wolips.targetbuilder.dist}/lib"/>
    </target>


    <!-- ============================================= -->
    <!-- Deletes temporary files.                      -->
    <!-- ============================================= -->
    <target name="clean">
        <delete dir="${build}/wolips-targetbuilder"/>
        <delete dir="${wolips.targetbuilder.dist}"/>
    </target>
    
    
    <!-- ============================================= -->
    <!-- Deploys WOLips web site files in a local      -->
    <!-- directory.                                    -->
    <!-- ============================================= -->
    <target name="deploy-web" depends="prepare,site-doc" if="deploy.web"/>
    
    
    <!-- ========================================== -->
    <!-- Builds API documenatation.                 -->
    <!-- ========================================== -->
    <target name="api" depends="prepare" if="wolips.build.targetbuilder">
    </target>
    
    
    <target name="doc" depends="prepare" if="wolips.build.targetbuilder">
        <!-- copy all licenses inclusing ours -->
        <copy todir="${wolips.targetbuilder.dist}/doc">
            <fileset dir="doc">
               <include name="LICENSE*"/>
            </fileset>
        </copy>
    </target>
    
    <target name="site-doc" depends="prepare" if="wolips.build.targetbuilder">

    </target>
</project>
