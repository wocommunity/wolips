<?xml version="1.0"?>

<!-- ======================================================== -->
<!--                 WOLips Ant file.                         -->
<!-- ======================================================== -->
<project name="woproject-wolips-logging" default="java" basedir=".">

    <!-- ============================================= -->
    <!-- Define CLASSPATH settings.                    -->
    <!-- ============================================= -->
    <target name="defineClasspath" if="wolips.build.logging">
       <path id="classpath">
            <fileset dir="${ant.home}/lib">
                <include name="ant.jar"/>
            </fileset>
            <fileset dir="lib">
                <include name="*.jar"/>
	            <include name="eclipse/**/*.jar"/>
            </fileset>
            <fileset dir="${wo.dir.library.frameworks}">
                <include name="JavaFoundation.framework/Resources/Java/*.jar"/>
            </fileset>
             <!-- Depends on WOProject core. -->
            <pathelement path="${build}/main"/>
        </path>	</target>

    <target name="java" depends="prepare" if="wolips.build.logging">
       <javac srcdir="src/wolips-logging/java"
                destdir="${build}/wolips-logging"
                deprecation="${compile.deprecation}"
                debug="${compile.debug}"
                optimize="${compile.optimize}">
            <classpath refid="classpath"/>
        </javac>
         
        <jar jarfile="${wolips.logging.dist}/lib/wolips-logging.jar">
            <fileset dir="${build}/wolips-logging"/>
            <fileset dir="src/wolips-logging/java">
          	    <include name="**/*.properties"/>
             </fileset>         
            <metainf dir="doc">
                <include name="LICENSE*"/>
            </metainf>
        </jar>
         <copy file="${woproject.dist}/lib/${lib.commons-logging}" tofile="${wolips.logging.dist}/lib/commons-logging.jar"/>
         <copy file="${woproject.dist}/lib/${lib.log4j}" tofile="${wolips.logging.dist}/lib/log4j.jar"/>
    </target>

    <target name="dist" depends="prepare,java,doc,api" if="wolips.build.logging">
        <copy todir="${wolips.logging.dist}">
             <fileset dir="src/wolips-logging">
             	<exclude name="**/java/**"/>
             	<exclude name="**/lib/**"/>
             	<exclude name="**/xdocs/**"/>
             	<exclude name="**/docs/**"/>
             	<exclude name=".project"/>
             	<exclude name=".classpath"/>
             	<exclude name="build.properties"/>
             </fileset>
         </copy>
      <jar jarfile="${wolips.logging.dist}.jar">
     	<fileset dir="${wolips.logging.dist}">
     	</fileset>
     </jar>
         
    </target>
         
         
         
    <!-- ============================================= -->
    <!-- Sets up build and distribution directories.   -->
    <!-- ============================================= -->
    <target name="prepare" depends="defineClasspath" if="wolips.build.logging">
        <mkdir dir="${build}/wolips-logging"/>
        <mkdir dir="${wolips.logging.dist}/lib"/>
    </target>


    <!-- ============================================= -->
    <!-- Deletes temporary files.                      -->
    <!-- ============================================= -->
    <target name="clean">
        <delete dir="${build}/wolips-logging"/>
        <delete dir="${wolips.logging.dist}"/>
    </target>
    
    
    <!-- ============================================= -->
    <!-- Deploys WOLips web site files in a local      -->
    <!-- directory.                                    -->
    <!-- ============================================= -->
    <target name="deploy-web" depends="prepare,site-doc" if="deploy.web"/>
    
    
    <!-- ========================================== -->
    <!-- Builds API documenatation.                 -->
    <!-- ========================================== -->
    <target name="api" depends="prepare" if="wolips.build.logging">
    </target>
    
    
    <target name="doc" depends="prepare" if="wolips.build.logging">
    </target>
    
    <target name="site-doc" depends="prepare" if="wolips.build.logging">
    </target>
</project>
