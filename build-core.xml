<?xml version="1.0"?>

<!-- ======================================================== -->
<!--              Main core WOProject Ant build file          -->
<!-- ======================================================== -->
<project name="woproject-core" default="java" basedir=".">      

    <!-- ============================================= -->
    <!-- Creates WOProject distribution.               -->
    <!-- ============================================= -->
    <target name="dist" depends="prepare,java,doc,api">
         <!-- Copy source code -->
         <copy todir="${woproject.dist}/src">
             <fileset dir="src">
             	<exclude name="**/wolips*/**"/>
             </fileset>
         </copy>

         <!-- Copy examples code -->
         <copy todir="${woproject.dist}/examples">
             <fileset dir="tests/wo">
             	<exclude name="**/build/**"/>
             	<exclude name="**/dist/**"/>
             	<exclude name="**/auto/**"/>
             </fileset>
         </copy>

         <tar tarfile="${dist.base}/${woproject.name}-src-${woproject.version}.tar">
             <tarfileset dir="${dist.base}">
                <include name="${woproject.name}-${woproject.version}/**"/>
                <exclude name="**/examples/**"/>
                <exclude name="**/lib/ant.jar"/>
                <exclude name="**/lib/${lib.junit}"/>
            </tarfileset>
         </tar>
         
         <gzip src="${dist.base}/${woproject.name}-src-${woproject.version}.tar"
              zipfile="${dist.base}/${woproject.name}-src-${woproject.version}.tar.gz"/>
         <delete file="${dist.base}/${woproject.name}-src-${woproject.version}.tar"/>
    </target>


    <!-- ============================================= -->
    <!-- Compiles Java classes                         -->
    <!-- ============================================= -->    
    <target name="java">
		<ant antfile="build-code.xml"/>
    </target>

    <!-- ============================================= -->
    <!-- Runs Unit tests.                              -->
    <!-- ============================================= -->
    <target name="test" depends="prepare,java">
        <ant antfile="build-test.xml"/>
    </target>
    

    <!-- ============================================= -->
    <!-- Sets up build and distribution directories.   -->
    <!-- ============================================= -->
    <target name="prepare">
        <mkdir dir="${woproject.dist}"/>
        <mkdir dir="${woproject.dist}/src"/>
        <mkdir dir="${woproject.dist}/examples"/>
    </target>


    <!-- ============================================= -->
    <!-- Deletes temporary files.                      -->
    <!-- ============================================= -->
    <target name="clean">        
        <ant antfile="build-test.xml" target="clean"/>
        <ant antfile="build-code.xml" target="clean"/>
        <ant antfile="build-doc.xml" target="clean"/>
                
        <delete dir="${woproject.dist}"/>
        <delete dir="${build}/main"/>
    </target>
    
    

    <!-- ========================================== -->
    <!-- Builds WOProject documenatation.           -->
    <!-- ========================================== -->
    <target name="doc">
		<ant antfile="build-doc.xml" target="doc"/>
    </target>


    <!-- ========================================== -->
    <!-- Builds API documenatation.                 -->
    <!-- ========================================== -->
    <target name="api">
		<ant antfile="build-doc.xml" target="api"/>
    </target>
    
    
    <!-- ============================================= -->
    <!-- Deploys WOProject web site in a local         -->
    <!-- directory.                                    -->
    <!-- ============================================= -->
    <target name="deploy-web" depends="java,doc,api" if="deploy.web">
		<ant antfile="build-doc.xml" target="deploy-web"/>
    </target>
</project>
