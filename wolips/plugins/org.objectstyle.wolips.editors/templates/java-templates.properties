Templates.action.name=action
Templates.action.desc=action to return a page
Templates.action.content=public ${return_type} ${name}() {\n			${return_type} page = (${return_type})this.pageWithName(${return_type}.class.getName());\n			${cursor}\n			return page;\n			}
		
Templates.actionthis.name=action.this
Templates.actionthis.desc=action to return this page
Templates.actionthis.content=public ${primary_type_name} ${name}() {\n        		${cursor}\n			return (${primary_type_name})this.context().page();\n			}

Templates.ecsessiondefault.name=ec session default
Templates.ecsessiondefault.desc=editing context from the session
Templates.ecsessiondefault.content=EOEditingContext ${name} = session().defaultEditingContext();\n        		${cursor}

Templates.ecnew.name=ec new
Templates.ecnew.desc=new editing context
Templates.ecnew.content=EOEditingContext ${name} = new EOEditingContext();\n        		${cursor}
