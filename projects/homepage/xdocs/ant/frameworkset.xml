<?xml version="1.0"?>
<document>
    <title>FrameworkSet Structure Manual</title>
<body>

<section name="FrameworkSet Structure"> 
		<h3>Description:</h3>
		<p><code>FrameworkSet</code> is an extension of  <a
			href="http://ant.apache.org/manual/CoreTypes/fileset.html" target="_blank">Ant FileSet</a>
			that allows matching and conditional inclusion of frameworks. It is used as a nested element 
			by various WOProject tasks. When building a FrameworkSet, path should match up to the 
            "*.framework" directory (no need to match individual JAR files).</p>
			
		<h3>Parameters:</h3>
  
            <table border="1" cellpadding="2" cellspacing="0">
				<tr>
					<th valign="top"><b>Attribute</b></th>
					<th valign="top"><b>Description</b></th>
					<th align="center" valign="top"><b>Required</b></th>
				</tr>
				<tr>
					<td valign="top">dir</td>
					<td valign="top">A base directory for the include/exclude pattern lookup. When the prefix
					for the directory matches one of <code>wo.root</code>, <code>wo.localroot</code> etc.
					this prefix will get replaced in the final class file by <code>WOROOT</code>, <code>LOCALROOT</code> 
					etc. and these paths in turn will get translated by the environment variables in the startup script
					to the final path.
					</td>
					<td valign="top" align="center">Yes</td>
				</tr>
				<tr>
					<td valign="top">deploymentDir</td>
					<td valign="top">The base directory that is prefixed to the frameworks in the classpath files. 
					This allows you to decouple your compile time paths from the deployment paths. Useful when you 
					have wastly different file layouts in the development vs. the deployment server. Used only 
					when inside a <code>woapplication</code> task.
					</td>
					<td valign="top" align="center">No</td>
				</tr>
				<tr>
					<td valign="top">if</td>
					<td valign="top">The name of a property that determines if the framework set is included. 
					If present, this property is used for conditional inclusion of the matched framework. 
					Property name should not be included in <code>${}</code>, as a property name, not value, is 
					expected.
					</td>
					<td valign="top" align="center">No</td>
				</tr>
			</table>
			<p><i>Note: all standard FileSet parameters are supported as well.</i></p>
	<p> </p>
 	<h4>Examples:</h4>
	<p>Using nested "frameworks" element that is a FrameworkSet inside WOCompile task:</p>
<source>&amp;lt;wocompile srcdir=&amp;quot;Java&amp;quot; destdir=&amp;quot;classes&amp;quot;&amp;gt;

   &amp;lt;frameworks dir=&amp;quot;../Frameworks&amp;quot;&amp;gt;
      &amp;lt;include name=&amp;quot;JavaWOExtension.framework&amp;quot;/&amp;gt;
      &amp;lt;include name=&amp;quot;ER*.framework&amp;quot;/&amp;gt;
      &amp;lt;exclude name=&amp;quot;ERNeutralLook.framework&amp;quot;/&amp;gt;
   &amp;lt;/frameworks&amp;gt;
   
   &amp;lt;frameworks dir=&amp;quot;${wo.localroot}&amp;quot;&amp;gt;
      &amp;lt;include name=&amp;quot;Mysql*.framework&amp;quot;/&amp;gt;
   &amp;lt;/frameworks&amp;gt;
   
 &amp;lt;/wocompile&amp;gt;
  </source>
    	</section>
    </body>
</document>
