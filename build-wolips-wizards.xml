<?xml version="1.0"?>

<!-- ======================================================== -->
<!--                 WOLips Ant file.                         -->
<!-- ======================================================== -->
<project name="woproject-wolips-wizards" default="java" basedir=".">

    <!-- ============================================= -->
    <!-- Define CLASSPATH settings.                    -->
    <!-- ============================================= -->
    <target name="defineClasspath" if="wolips.build.wizards">
        <path id="classpath">
            <fileset dir="${ant.home}/lib">
                <include name="ant.jar"/>
            </fileset>
            <fileset dir="lib">
                <include name="*.jar"/>
	            <include name="eclipse/**/*.jar"/>
            </fileset>
	    <fileset dir="${wolips.dist}">
			<include name="**/wolips.jar"/>
	    </fileset>
            <fileset dir="${wo.dir.library.frameworks}">
                <include name="JavaFoundation.framework/Resources/Java/*.jar"/>
            </fileset>
             <!-- Depends on WOProject core. -->
            <pathelement path="${build}/main"/>
        </path>
    </target>

    <target name="java" depends="prepare" if="wolips.build.wizards">
        <javac srcdir="src/wolips-wizards/java"
                destdir="${build}/wolips-wizards"
                deprecation="${compile.deprecation}"
                debug="${compile.debug}"
                optimize="${compile.optimize}">
            <classpath refid="classpath"/>
        </javac>
         
        <jar jarfile="${wolips.wizards.dist}/lib/wolips-wizards.jar">
            <fileset dir="${build}/wolips-wizards"/>
            <fileset dir="src/wolips-wizards/java">
          	    <include name="**/*.properties"/>
             </fileset>         
            <metainf dir="doc">
                <include name="LICENSE*"/>
            </metainf>
        </jar>
        
    </target>

    <target name="dist" depends="prepare,java,doc,api" if="wolips.build.wizards">
         <copy todir="${wolips.wizards.dist}">
             <fileset dir="src/wolips-wizards">
             	<exclude name="**/java/**"/>
             	<exclude name="**/lib/**"/>
             	<exclude name="**/xdocs/**"/>
             	<exclude name="**/docs/**"/>
             	<exclude name=".project"/>
             	<exclude name=".classpath"/>
             	<exclude name="build.properties"/>
             </fileset>
         </copy>
     <jar jarfile="${wolips.wizards.dist}.jar">
     	<fileset dir="${wolips.wizards.dist}">
     	</fileset>
     </jar>
          
    </target>
         
         
         
    <!-- ============================================= -->
    <!-- Sets up bwizardsld and distribution directories.   -->
    <!-- ============================================= -->
    <target name="prepare" depends="defineClasspath" if="wolips.build.wizards">
        <mkdir dir="${build}/wolips-wizards"/>
        <mkdir dir="${wolips.wizards.dist}/lib"/>
    </target>


    <!-- ============================================= -->
    <!-- Deletes temporary files.                      -->
    <!-- ============================================= -->
    <target name="clean">
        <delete dir="${build}/wolips-wizards"/>
        <delete dir="${wolips.wizards.dist}"/>
    </target>
    
    
    <!-- ============================================= -->
    <!-- Deploys WOLips web site files in a local      -->
    <!-- directory.                                    -->
    <!-- ============================================= -->
    <target name="deploy-web" depends="prepare,site-doc" if="deploy.web"/>
    
    
    <!-- ========================================== -->
    <!-- Bwizardslds API documenatation.                 -->
    <!-- ========================================== -->
    <target name="api" depends="prepare" if="wolips.build.wizards">
    </target>
    
    
    <target name="doc" depends="prepare" if="wolips.build.wizards">
    </target>
    
    <target name="site-doc" depends="prepare" if="wolips.build.wizards">
    </target>
</project>
